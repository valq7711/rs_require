dist: xenial
addons:
  chrome: stable
before_install:
  - "python -m http.server --directory test/"
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost/test.html &
